# 🔒 보안 가이드

## 📋 목차
1. [현재 보안 상태](#현재-보안-상태)
2. [즉시 해야 할 것](#즉시-해야-할-것)
3. [API 키 관리](#api-키-관리)
4. [추가 보안 권장사항](#추가-보안-권장사항)
5. [보안 점검 체크리스트](#보안-점검-체크리스트)

---

## 🛡️ 현재 보안 상태

### ✅ 구현된 보안 기능:
- ✅ **HTTPS 암호화** (GitHub Pages 기본)
- ✅ **Firebase Authentication** (관리자 로그인)
- ✅ **XSS 공격 방어** (입력값 검증 및 이스케이프)
- ✅ **Rate Limiting** (클라이언트 측 제한)
- ✅ **입력값 검증** (길이, 형식, 위험 패턴 차단)
- ✅ **관리자 경로 숨김** (`secure-admin-xk9p2v`)
- ✅ **robots.txt** (검색엔진 차단)

### ⚠️ 주의가 필요한 부분:
- ⚠️ Firebase API 키 클라이언트 노출 (정상, 하지만 Security Rules로 보호 필요)
- ⚠️ imgBB API 키 노출 (무료 서비스이므로 큰 문제 없음)
- ⚠️ 관리자 비밀번호 (강력하게 변경 권장)

---

## 🚨 즉시 해야 할 것 (중요!)

### 1. Firebase Security Rules 설정 (필수!)

**⚠️ 지금 당장 설정하지 않으면 데이터가 위험합니다!**

#### 단계:
1. https://console.firebase.google.com/ 접속
2. 프로젝트 선택 (curtainweb)
3. **Firestore Database** → **규칙 (Rules)** 클릭
4. 아래 규칙 복사 후 붙여넣기
5. **게시** 버튼 클릭

#### 안전한 보안 규칙:
```javascript
rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    
    // 제품 컬렉션
    match /products/{productId} {
      allow read: if true;  // 모든 사람 읽기 가능
      allow write: if request.auth != null;  // 로그인한 관리자만 쓰기
      
      // 데이터 검증
      allow create, update: if request.auth != null
        && request.resource.data.name is string
        && request.resource.data.name.size() > 0
        && request.resource.data.name.size() < 100
        && request.resource.data.category in ['curtain', 'blind'];
    }
    
    // 시공사례 컬렉션
    match /portfolios/{portfolioId} {
      allow read: if true;
      allow write: if request.auth != null;
      
      allow create, update: if request.auth != null
        && request.resource.data.title is string
        && request.resource.data.title.size() > 0
        && request.resource.data.title.size() < 200;
    }
    
    // 문의 컬렉션
    match /inquiries/{inquiryId} {
      // 관리자만 읽기
      allow read: if request.auth != null;
      
      // 모든 사람이 문의 등록 가능 (클라이언트 Rate Limiting 적용)
      allow create: if request.resource.data.name is string
        && request.resource.data.name.size() > 0
        && request.resource.data.name.size() < 51
        && request.resource.data.phone is string
        && request.resource.data.phone.size() > 0
        && request.resource.data.message is string
        && request.resource.data.message.size() > 0
        && request.resource.data.message.size() < 1001;
      
      // 수정/삭제는 관리자만
      allow update, delete: if request.auth != null;
    }
    
    // 메인 이미지
    match /mainImages/{imageId} {
      allow read: if true;
      allow write: if request.auth != null;
    }
    
    // 사이트 텍스트
    match /siteTexts/{textId} {
      allow read: if true;
      allow write: if request.auth != null;
    }
    
    // 기타 모든 컬렉션 차단
    match /{document=**} {
      allow read, write: if false;
    }
  }
}
```

---

### 2. 관리자 비밀번호 변경 (권장!)

**현재 비밀번호 `admin1234`는 너무 약합니다!**

#### 단계:
1. https://console.firebase.google.com/ 접속
2. **Authentication** → **Users** 클릭
3. `admin@curtainweb.com` 클릭
4. **비밀번호 재설정** 클릭
5. 강력한 비밀번호로 변경

#### 강력한 비밀번호 예시:
```
✅ CurtainWeb2024!@#SecurePass
✅ MyC0mpl3x#Pass2024!
✅ S3cur3P@ssw0rd#2024

❌ admin1234
❌ password123
❌ 1234567890
```

#### 비밀번호 조건:
- ✅ 최소 12자 이상
- ✅ 대문자, 소문자, 숫자, 특수문자 포함
- ✅ 추측하기 어려운 조합
- ✅ 개인정보 미포함

---

## 🔑 API 키 관리

### Firebase API 키

**Q: Firebase API 키가 클라이언트에 노출되는데 괜찮나요?**  
**A: 네, 정상입니다.** Firebase는 클라이언트 측 인증을 위해 설계되었습니다.

#### 보호 방법:
1. ✅ **Firebase Security Rules** (가장 중요!)
2. ✅ Firebase Console → 프로젝트 설정 → 앱 제한
3. ✅ 허용된 도메인만 설정:
   - `nacadmic.github.io`
   - `localhost` (개발용)

#### 설정 방법:
```
1. Firebase Console → 프로젝트 설정 (⚙️)
2. "내 앱" 섹션 → Web 앱 선택
3. "앱 제한" → "HTTP 리퍼러" 추가
   - nacadmic.github.io/*
   - localhost/*
```

---

### imgBB API 키

**현재 상태:** 클라이언트에 노출됨

#### 영향:
- ⚠️ 누군가 키를 복사해서 사용할 수 있음
- ✅ 무료 서비스이므로 금전적 피해 없음
- ✅ 관리자 페이지만 사용하므로 일반 사용자는 접근 불가

#### 개선 방법 (선택):
```
1. imgBB에서 API 키 재생성
2. secure-admin-xk9p2v 폴더에만 키 포함
3. 또는 Firebase Functions로 이미지 업로드 처리 (복잡함)
```

**권장:** 현재 상태 유지 (큰 문제 없음)

---

## 🛡️ 추가 보안 권장사항

### 1. 정기적인 보안 점검

#### 월 1회 확인:
- [ ] Firebase 사용량 확인 (이상 트래픽 감지)
- [ ] 관리자 로그인 기록 확인
- [ ] 문의 데이터 확인 (스팸 여부)
- [ ] 의심스러운 활동 모니터링

#### 분기 1회 확인:
- [ ] 비밀번호 변경
- [ ] Firebase Security Rules 검토
- [ ] API 키 갱신 (필요시)
- [ ] 백업 확인

---

### 2. 백업 전략

#### Firestore 백업:
```
1. Firebase Console → Firestore Database
2. "가져오기/내보내기" → "내보내기"
3. Google Cloud Storage 버킷 선택
4. 자동 백업 설정 (유료 기능)
```

#### 수동 백업 (무료):
- 중요한 데이터는 엑셀로 내보내기
- 정기적으로 스크린샷 저장

---

### 3. 모니터링

#### Firebase Console에서 확인:
- **Authentication** → 로그인 기록
- **Firestore** → 사용량 통계
- **Analytics** → 트래픽 분석 (Google Analytics 연동 시)

#### 이상 징후:
- ⚠️ 급격한 트래픽 증가
- ⚠️ 알 수 없는 로그인 시도
- ⚠️ 비정상적인 데이터 패턴
- ⚠️ 무료 한도 초과 경고

**대응:** 즉시 비밀번호 변경 및 Security Rules 점검

---

### 4. HTTPS 강제

**현재 상태:** GitHub Pages는 자동으로 HTTPS 적용 ✅

#### 확인 방법:
- URL이 `https://`로 시작하는지 확인
- 브라우저 주소창에 자물쇠 🔒 아이콘 표시

---

### 5. 관리자 계정 보호

#### 권장사항:
- ✅ 비밀번호 정기 변경 (3개월마다)
- ✅ 비밀번호 관리자 사용 (LastPass, 1Password 등)
- ✅ 2단계 인증 활성화 (Firebase에서 지원)
- ❌ 공용 컴퓨터에서 로그인 금지
- ❌ 로그인 상태 유지 금지 (공용 기기)

#### 2단계 인증 설정:
```
1. Firebase Console → Authentication
2. "로그인 방법" → "다단계 인증"
3. "사용 설정" 클릭
4. Google Authenticator 앱으로 QR 코드 스캔
```

---

## ✅ 보안 점검 체크리스트

### 초기 설정 (필수!)
- [ ] Firebase Security Rules 설정
- [ ] 관리자 비밀번호 변경
- [ ] Firebase 앱 제한 (도메인 설정)
- [ ] HTTPS 확인

### 월간 점검
- [ ] Firebase 사용량 확인
- [ ] 로그인 기록 확인
- [ ] 스팸 문의 확인
- [ ] 백업 수행

### 분기 점검
- [ ] 비밀번호 변경
- [ ] Security Rules 검토
- [ ] 보안 업데이트 확인
- [ ] 모니터링 설정 확인

---

## 🚨 보안 사고 대응

### 의심스러운 활동 발견 시:

1. **즉시 조치:**
   ```
   1. Firebase Console → Authentication
   2. 모든 사용자 세션 종료
   3. 비밀번호 즉시 변경
   4. Security Rules 점검
   ```

2. **데이터 확인:**
   ```
   1. Firestore에서 최근 변경사항 확인
   2. 의심스러운 데이터 삭제
   3. 백업에서 복구 (필요시)
   ```

3. **API 키 갱신:**
   ```
   1. Firebase API 키 재생성
   2. imgBB API 키 재생성
   3. 모든 소스코드 업데이트
   ```

4. **보안 강화:**
   ```
   1. Security Rules 더 엄격하게 설정
   2. Rate Limiting 강화
   3. 2단계 인증 활성화
   ```

---

## 📞 문의

보안 관련 질문이나 의심스러운 활동 발견 시:
1. Firebase Support 문의
2. GitHub Issues에 보안 태그로 등록
3. 개발자에게 직접 연락

---

## 📚 참고 자료

- [Firebase Security Rules 공식 문서](https://firebase.google.com/docs/firestore/security/get-started)
- [OWASP Top 10 (웹 보안)](https://owasp.org/www-project-top-ten/)
- [Google Cloud Security Best Practices](https://cloud.google.com/security/best-practices)

---

**마지막 업데이트:** 2024년 11월

**작성자:** 유명커튼블라인드 개발팀

